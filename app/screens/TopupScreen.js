import React, {useState, useEffect} from 'react';
import {
  Box,
  Text,
  HStack,
  Heading,
  VStack,
  Center,
  Avatar,
  Button,
  Input,
  Spinner,
  FlatList,
  Divider,
  Fab,
  ScrollView,
  Icon,
  Modal,
} from 'native-base';

export default function Topup({navigation, route}) {
  return <Box flex={1} bgColor={'#FFF2F2'} p={10}></Box>;
}
